rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side read/write access to all collections.
    // Only server-side code using the Firebase Admin SDK can access the data.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}